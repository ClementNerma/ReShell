[package]
name = "reshell-checker"
version = "0.1.0"
edition = "2021"

[dependencies]
parsy = "0.6.5"
reshell-parser = { path = "../parser" }
reshell-shared = { version = "0.1.0", path = "../shared" }
